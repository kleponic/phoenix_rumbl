<h1>List of User</h1>
<table class="table">
    <%= for user <- @users do %>
    <tr>
        <td><%= first_name(user) %> __ <%= user.id %></td>
        <td><%= link "View", to: user_path(@conn, :show, user.id) %></td>
    </tr>
    <%= end %>
</table>